using System;

public class Book
{
    public string Title;
    public int Page;
    public bool Finished;

    public Book(string title, int page, bool finished)
    {
        Title = title;
        Page = page;
        Finished = finished;
    }
}
